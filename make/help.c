#include <stdio.h>

int main (void){

printf("strlength: finds the length of a string \n strconcat: merges strings \n strcompare: compares two strings\n \n To run any of the 3 run the .out files with the apropriate name.  ");


return 0;
}